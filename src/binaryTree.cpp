#include<iostream>
#include<vector>
#include<algorithm>
struct Node{
    Node *left;
    Node *right;
    int value;
};

bool firstOrderInit(Node * baseNode,vector<int>& firOrdV){
    try{
        if(firOrdV.size() == 0)
            return false;
        // for (auto & i : firOrdV){
        //     if(i.)
        // }
        
    }
    catch(const std::exception& e)
    {

    }
}
